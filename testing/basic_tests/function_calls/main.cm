typ main :: Int
def main = {
    def y :: Int = 3;
    def a :: Int = add 2 y;
    def res :: Int = if equals y a then 2 else 3;
    res
}

typ add :: Int -> Int -> Int
def add x y =
    x + y

typ equals :: Int -> Int -> Bool
def equals x y =
    if x == y then true else false
