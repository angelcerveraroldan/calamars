-- A simple and minimial file that should lower to MIR without any issues!

--*
Given $a, b in ZZ$ return $a + b$
*--
def add(a: Int, b: Int): Int  = a + b

def mul(a: Int, b: Int): Int = a * b

def mod(a: Int, m: Int): Int = {
		-- Calculate a mod m
		val modulo: Int = a % m;
		-- Maybe we do something unecessary here...
		val nothing: Int = 0;
		-- Now we return the value
		modulo
}

-- If statements
def is_even(a: Int): Bool = 
	if a % 2 == 0
		then true
		else false
